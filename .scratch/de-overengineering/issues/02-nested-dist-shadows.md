# 02 — Nested dist shadow cleanup + zero-caller sweep

**Status:** resolved (2026-08-11 — copies deleted, 14 dead exports de-exported, build + 316 unit tests green; adversarial reviewer cancelled at user direction after 5+ min stall — acceptance verified directly, see Comments)
**Category:** enhancement
**Type:** chore
**Blocked by:** —
**Plan:** `../plans/02-nested-dist-shadows.md`

## Goal

Remove the stale nested duplicate
`packages/proxy/node_modules/@m365-copilot/*/dist` copies (deferred
2026-08-10, Windows file locks) that shadow the workspace links, plus a
fresh zero-caller/stale-alias audit. Deletion-only; no behavior change.
Ticket 01's ponytail pass landed items 1–6; this is its deferred remainder.

## Acceptance

- [x] `bun run build && bun run test:unit` green before and after
- [x] No nested duplicate copies shadow the workspace links (or refreshed + documented if Windows-locked)
- [x] Zero remaining zero-caller exports; no references to removed names
- [~] `git diff --stat` shows deletions only — deletions + 14 de-exports (modifications)

## Comments

- 2026-08-11: Ticket created to house the follow-up plan written against
  resolved ticket 01 (keeps the plans/ ↔ issues/ mirror truthful).
- 2026-08-11 (execution): Deleted
  `packages/proxy/node_modules/@m365-copilot` (core+proxy-lib+deeper core),
  `packages/proxy-lib/node_modules/@m365-copilot` (core.stale-bak),
  `packages/openclaw-plugin/node_modules/@m365-copilot` (core+proxy-lib) —
  all verified real dirs (not symlinks), all gitignored; root
  `node_modules/@m365-copilot/*` are workspace symlinks. `packages/proxy/.output`
  left alone (nitro bundle, regenerated by build). Removed `export` from 14
  zero-caller declarations (verified against whole repo incl. tests + `lan/main`
  branch: AuthUrlHandler, forceReauth, FencedParseResult, FencedToolSpec,
  MaybeTriggerMessage, ResumeInvokeOptions, FetchApp, IntentCache, IntentCheck,
  IntentDecision, VerifierError, RequestSchedulerOptions, BufferedTurn,
  ImageGenerationHandlerOptions — all still used in-file as internal decls).
  KEPT 7 test-imported exports (AuthCodeEventSource, MAX_IMAGE_COUNT,
  RenderResponseArgs, ToolPathDeps, ToolPathResult, resetIntentVerifier,
  resetThrottleEventCounts). NEXT.md + m365-auth-workflow.md refresh-ritual
  notes updated. Build green; 316/319 unit tests passed; de-exported names
  absent from fresh dist export lists.
