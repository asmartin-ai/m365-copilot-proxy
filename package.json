{
  "name": "m365-copilot-proxy",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "dev": "bun run --cwd packages/proxy dev",
    "build": "bun run --cwd packages/core build && bun run --cwd packages/proxy-lib build && bun run --cwd packages/proxy build && bun run --cwd packages/openclaw-plugin build",
    "proxy": "bun packages/proxy/bin/m365-proxy.mjs",
    "pi": "bash scripts/pi-local.sh",
    "test": "bun run test:unit",
    "test:unit": "bun run build && bun run vitest run",
    "test:live": "bun run build && M365_LIVE=1 bun run vitest run"
  },
  "devDependencies": {
    "tsdown": "^0.21.5",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
